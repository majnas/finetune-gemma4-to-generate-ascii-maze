# Copy to .env and adjust for your machine.

# Base dir for ALL Hugging Face cache (models/hub, datasets, tokens, etc).
# Routes it to the user's home instead of a shared/permission-locked cache.
HF_CACHE_DIR=~/.cache/huggingface
